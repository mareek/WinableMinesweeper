<Page
    x:Class="WinableMinesweeper.MineFieldPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinableMinesweeper"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Button Name="FlagButton" HorizontalAlignment="Center" Click="FlagButton_Click">FLAG</Button>
        <TextBlock Name="DebugTextBlock" HorizontalAlignment="Center" Grid.Row="1"></TextBlock>
        <Viewbox Grid.Row="2">
            <Border BorderBrush="Black" BorderThickness="1">
                <Grid Name="MineGrid" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

                </Grid>
            </Border>
        </Viewbox>
    </Grid>
</Page>
